{{!--
Partial: Tout les produits de la page blog
--}}

<produits>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h2 id="texte">Nos véhicules en ventes</h2>
            </div>
            <label class="txt-color-black">
                <i class="fas fa-sort"></i>
                Trier par :
                <select onchange="getValue()" id="monSelecteur" class="form-select-sm txt-color-black">
                    <option>Plus récent</option>
                    <option value="croissant">Prix : croissant</option>
                    <option value="décroissant">Prix : décroissant</option>
                </select>
            </label>
        </div>
        <div id="voitures" class="product row pb-3 justify-content-center">
            {{#each voiture}}
            <div id="voiture-{{this.id}}" class="col-12 col-md-6 col-lg-4">
                <div class="product-item">
                    <a href="/blog/{{this.id}}">
                        <img src="{{this.img_url}}">
                    </a>
                    <h3>{{ this.make }} {{ this.model }}</h3>
                    <p>Prix: {{ this.price }}€</p>
                    {{!-- <p>{{ this.id }}</p> --}}
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</produits>